RAY_VERSION = "ray.version"
"""The version of the Ray runtime."""

RAY_NODE_ID = "ray.node.id"
"""The unique identifier of the Ray node."""

RAY_NODE_CPU_COUNT = "ray.node.cpu_count"
"""The number of CPUs available on the Ray node."""

RAY_NODE_GPU_COUNT = "ray.node.gpu_count"
"""The number of GPUs available on the Ray node."""

RAY_JOB_ID = "ray.job.id"
"""The unique identifier of the Ray job."""

RAY_WORKER_ID = "ray.worker.id"
"""The unique identifier of the Ray worker."""

RAY_WORKER_PROCESS_TYPE = "ray.worker.process_type"
"""The type of the Ray worker process (e.g., worker, driver)."""

RAY_ACTOR_ID = "ray.actor.id"
"""The unique identifier of the Ray actor."""

RAY_NAMESPACE = "ray.namespace"
"""The namespace the Ray runtime is operating in."""

RAY_CLUSTER_SESSION_NAME = "ray.cluster.session_name"
"""The session name of the Ray cluster."""

RAY_CLUSTER_GCS_ADDRESS = "ray.cluster.gcs_address"
"""The address of the Ray cluster's Global Control Store (GCS)."""
